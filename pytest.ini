[pytest]
asyncio_mode = auto
; Bare `pytest` from the repo root must not collect tools/android_assets —
; those tests need the separate tools/android_assets/requirements-export.txt env.
testpaths = tests
